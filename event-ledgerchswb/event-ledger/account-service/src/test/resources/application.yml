server:
  port: 0

spring:
  application:
    name: account-service-test
  datasource:
    url: jdbc:h2:mem:testaccountdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
